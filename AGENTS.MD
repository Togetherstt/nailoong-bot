# AGENTS.md

本仓库用于开发和长期维护一个基于 `NapCatQQ` 的自定义 QQ 机器人项目。核心目标是围绕 `NoneBot2 + OneBot V11` 构建可持续维护的 QQ 机器人逻辑层，并逐步扩展到图片处理、多模态接口、文本改写、消息库存与群聊互动玩法。

本文件既是开发约定，也是后续 AI 代码助手长期协作的默认执行手册。除非明确改口，默认技术栈为：

- `Python 3.10+`
- `NoneBot2`
- `OneBot V11`
- `NapCatQQ`

项目按“通信宿主层”和“业务逻辑层”分离：

- 通信层由 `NapCatQQ` 独立运行处理，不在此代码库范围内。
- 本代码库只负责通过 WebSocket 接收事件流、处理业务逻辑并返回指令。

默认范围是单点部署、轻量级插件化架构。除非后续状态规模明显增大，否则优先使用本地文件持久化，不主动引入数据库。

## 1. 项目概述

这是一个典型的事件驱动型异步 Python 项目。为了避免插件增多后出现状态耦合、密钥泄露、依赖混乱和消息阻塞，`AGENTS.md` 持续作为工程规范说明存在。

当前版本已经不再处于“只有骨架”的阶段，而是一个可运行、可持久化、带权限控制和本地测试能力的多插件机器人项目。

## 2. 仓库结构

默认仓库结构按长期维护与 NoneBot2 插件化规范组织：

```text
Nailoong Bot/
├─ AGENTS.MD
├─ README.md
├─ LICENSE
├─ bot.py
├─ pyproject.toml
├─ requirements.txt
├─ .env.example
├─ data/
│  ├─ group_daily_stats/
│  ├─ homophone_box/
│  ├─ joke_library/
│  ├─ nailoong/
│  ├─ sts_card_guess/
│  └─ xilian_style/
├─ scripts/
├─ src/
│  └─ plugins/
│     ├─ basic_reply/
│     ├─ group_daily_stats/
│     ├─ help_center/
│     ├─ homophone_box/
│     ├─ joke_library/
│     ├─ nailoong_memory/
│     ├─ sts_card_guess/
│     ├─ utils/
│     ├─ vision_api/
│     └─ xilian_style/
└─ tests/
```

硬性约定：

- 后续新增代码时，必须按照 `src/plugins/` 隔离具体业务。
- 不接受把所有命令堆进同一个 `.py` 文件。
- 新增纯逻辑时，优先补充 `tests/` 中对应单元测试。

## 3. 快速命令

### 环境初始化

```bash
pip install -r requirements.txt
```

或：

```bash
poetry install
```

### 启动机器人逻辑端

```bash
nb run --reload
```

或：

```bash
python bot.py
```

### 运行测试

```bash
python -m unittest discover -s tests -v
```

## 4. 环境变量约定

- 环境变量文件固定为 `.env`（本地测试）和可选的生产配置文件。
- API 密钥、访问令牌、管理员 QQ、群号白名单等敏感信息绝对禁止硬编码在代码中。
- 代码中只允许通过 `nonebot.get_driver().config` 读取配置。

建议维护的字段包括：

```env
ENVIRONMENT=dev
LOG_LEVEL=DEBUG
HOST=127.0.0.1
PORT=8080
ONEBOT_ACCESS_TOKEN=replace-with-your-token
ADMIN_QQ=123456789
EXTRA_PLUGIN_GROUP_IDS=123456789,987654321
HOMOPHONE_GROUP_IDS=123456789,987654321
VISION_API_URL=https://api.example.com/v1/chat/completions
VISION_API_KEY=sk-xxxxxx
XILIAN_API_URL=https://api.example.com/v1/responses
XILIAN_API_KEY=sk-xxxxxx
XILIAN_API_MODEL=gpt-5.4
XILIAN_API_TIMEOUT=20
STS_CARD_DATA_DIR=data/sts_card_guess/cards
```

## 5. 核心架构与逻辑分层

本项目没有传统意义上的“前端”，界面的呈现完全依赖 QQ 客户端。本仓库全权负责“后端”的事件处理。

### 核心子模块

- `src.plugins.basic_reply`
  - 基础表情与文字回复能力。
- `src.plugins.vision_api`
  - 图像识别与多模态 API 接入预留层。
- `src.plugins.utils.http_client`
  - 通用异步 HTTP 请求封装，负责超时、异常与重试控制。

### 消息处理流水线约定

1. **事件解析**
   - 将 OneBot 协议事件转换为明确的 Python 对象。
2. **意图路由**
   - 通过 NoneBot 的 Matcher，如 `on_command`、`on_message`、`on_regex` 等分发任务。
3. **异步处理**
   - 凡是涉及 IO 的操作，必须优先使用 `async/await`。
   - 禁止在消息处理函数里使用同步 `requests` 或长时间阻塞逻辑。
4. **结果组装与发送**
   - 将返回值格式化为文本、图片段或组合消息，再调用 `bot.send` / `send_group_msg` 发送。

## 6. 关键开发约定

以下规则是硬约束，违反后很容易导致机器人卡死、频繁掉线、刷屏、丢数据或后续难以扩展。

1. **绝对的异步优先**
   - 任何网络请求、文件读写、图片下载优先使用异步实现。
   - 不要在处理函数中写同步阻塞代码或死循环。
2. **敏感信息隔离**
   - API Key、访问令牌、管理员 QQ、特定群号必须放入 `.env`。
3. **防异常崩溃**
   - 第三方 API 调用必须带 `try...except` 和合理的超时。
   - 上游不可用时，机器人应返回降级提示，而不是直接异常退出。
4. **资源清理**
   - 涉及临时文件时，必须考虑异常分支下的清理。
   - 删除资源时，要同步维护索引和本地文件。
5. **权限控制**
   - 管理命令和高消耗接口必须依赖 `ADMIN_QQ`、白名单群或等价约束。
6. **插件隔离**
   - 新功能应尽量独立为单插件，不与其他插件状态无序耦合。
7. **文档同步**
   - 行为发生用户可感知变化时，`README.md` 与 `AGENTS.MD` 都要同步更新。
8. **群范围控制**
   - 当前版本默认对所有群开放：
     - 杀戮尖塔猜卡插件
     - 群每日统计插件
   - 其他插件仅允许在 `.env` 的 `EXTRA_PLUGIN_GROUP_IDS` 指定群中生效。

## 7. 日志与安全约定

日志中禁止出现以下内容：

- `API Key`
- `ONEBOT_ACCESS_TOKEN`
- 完整请求头
- 完整上游请求体
- 未脱敏的管理员 QQ、用户 QQ、群号
- 用户原始敏感消息全文

允许记录的内容应尽量限制为：

- 插件名
- 错误类型
- 状态码
- 模型名
- 脱敏后的上下文

调试要求：

- 如果必须调试请求内容，必须先脱敏。
- 本地临时调试打印应在完成排查后移除。
- 不要把含密钥的日志、截图或终端输出复制进文档、提交说明或聊天记录。

## 8. 本地开发与验证流程

标准闭环仍然是：

`配置 NapCatQQ -> 编写插件 -> 重启 bot.py -> 小号发消息验证 -> 查阅终端日志`

每次提交前至少验证这些点：

1. NapCatQQ 能正常连接到 Python 端 WebSocket。
2. 命令能被正确解析，且错误输入时不会崩溃。
3. 关键持久化数据在重启后仍可读。
4. 第三方 API 异常时会返回明确的降级提示。
5. 新增纯逻辑至少有一组单元测试。

## 9. 当前插件行为约定

### `nailoong_memory`

- 支持通过引用图片或可解析表情添加奶龙。
- 数据保存在 `data/nailoong/`。
- 管理命令仅管理员可用。
- 支持 `@机器人 /编号查找奶龙 <序号>` 与 `@机器人 /模糊查找奶龙 <名称关键字>`，查找范围只针对奶龙名称与库存序号。
- 支持群级开关：
  - `@机器人 /奶龙套皮`
  - `@机器人 /奶龙去皮`
- 套皮开启后，本群 `/随机奶龙` 改为以合并转发消息发送；关闭后恢复直接发送。
- 奶龙套皮群开关需要持久化，重启后继续生效。
- 去重策略为：
  - 原始字节 `sha256`
  - 感知哈希 `dHash`
  - 必要时补充消息段规范化指纹

### `homophone_box`

- 仅在 `EXTRA_PLUGIN_GROUP_IDS` 指定群中可用。
- 同时仍受 `HOMOPHONE_GROUP_IDS` 白名单限制。
- 首字母长度支持 `2` 到 `5`。
- 支持中文单字、英文首字母、英文整词命中。
- 命中结果超过 `10` 条时只返回 `10` 条。
- 同一引用消息有 `5` 秒冷却。
- 带有限制等待队列与并发保护。
- 若命中结果存在已绑定群友，统计插件会将这些命中同时计入“被 /盒 次数”。

### `xilian_style`

- 仅在 `EXTRA_PLUGIN_GROUP_IDS` 指定群中可用。
- 使用 Responses API 形态调用上游模型。
- 输入上限 `200` 字，输出上限 `200` 字。
- 输出必须以单个 `♪` 结尾。
- 模式开关仅管理员可控。

### `joke_library`

- 仅在 `EXTRA_PLUGIN_GROUP_IDS` 指定群中可用。
- 支持引用上传、tag 分类、随机抽取、模糊查找、编号查找、删除。
- `/删除笑话` 仅管理员可用。
- 随机笑话只展示内容与编号。
- 去重基于规范化消息内容指纹。
- 若群内直接出现命中“张老师”“张师”“张雪峰”“雪峰”“巧乐兹”“雪碧”的消息，则无需 `/上传笑话`，也要自动归档到 `张雪峰` tag，并正常提示入库结果。
- 若手动上传时引用内容命中上述关键词，也同样自动归档到 `张雪峰` tag。

### `sts_card_guess`

- 卡池默认读取 `data/sts_card_guess/cards/`，也可由 `STS_CARD_DATA_DIR` 覆盖。
- 当前为整池随机，不做来源过滤。
- 当前为所有群默认开放的主要游戏插件。
- 来源展示统一为：
  - `故障机器人`
  - `静默猎手`
  - `铁血战士`
  - `储君`
  - `亡灵契约师`
  - `无色`
  - `其他`
- 同群同一时间只允许一局。
- 开局展示 3 条基础提示，`10` 秒后补第 4 条。
- 之后每 `60` 秒揭示一次描述。
- 揭示量按开局隐藏字总数固定切成 `4` 批。
- 支持本地脚本 `scripts/test_sts_card_guess_local.py` 直接测试核心逻辑。

### `group_daily_stats`

- 对所有群默认可用，但只有管理员执行 `@机器人 /开启统计` 后才开始记数。
- 管理命令为：
  - `@机器人 /开启统计`
  - `@机器人 /结束统计`
  - `@机器人 /查看当前统计`
- 仅 `ADMIN_QQ` 可以开启或结束统计。
- 开启后会持续生效，直到管理员显式结束。
- 每天 `23:59:59` 自动向对应群发送当日统计。
- 自动发送后必须立即清空当天数据，次日重新开始。
- 统计按群隔离、按天隔离，不允许串群或跨天累积。
- 统计项固定为：
  - 被艾特次数最多的人
  - 贴表情最多的人
  - 被贴表情次数最多的人
  - 若当前群可用 `/盒`，则额外统计 `/盒` 次数最多的人
  - 若当前群可用 `/盒` 且命中已绑定群友，则额外统计被 `/盒` 次数最多的人
- “贴表情”当前按消息段类型 `face`、`mface`、`marketface` 统计。
- “被贴表情”当前按“同一条含表情消息里被 @ 的群成员”统计，不统计 `@all`，也不统计机器人自己。
- 统计过滤约定：若消息发送者或被 `@` 对象的群昵称/昵称为 `ARClass` 或 `Yurisaki`，则该成员相关的艾特与表情统计一律忽略，避免其他 bot 干扰统计结果。
- 数据保存在 `data/group_daily_stats/`。

### `help_center`

- 总帮助固定为 `@机器人 /help`。
- 若当前群不在 `EXTRA_PLUGIN_GROUP_IDS` 内，总帮助只展示猜卡帮助入口。
- 除 `/猜卡 help` 外，其他分插件帮助仅在 `EXTRA_PLUGIN_GROUP_IDS` 指定群可用。

## 10. 更新日志

本区域用于记录每次更新带来的新功能与具体修改细节，按照时间戳进行排列，只进行增量更新。历史日志应尽量保留；若涉及敏感信息，应做脱敏或替换为配置项名称。

### 2026-06-05 11:35

- 新增需求：实现“群每日统计”插件，按群统计当日互动数据，并在每天 `23:59:59` 自动播报后清空。
- 权限约定：仅 `ADMIN_QQ` 可在群内通过 `@机器人 /开启统计` 与 `@机器人 /结束统计` 控制统计开关；非管理员开启时统一回复“您没有管理权限，无法开启”，结束时统一回复“您没有管理权限，无法结束”。
- 查询约定：任意群成员可通过 `@机器人 /查看当前统计` 查看当天截至当前时刻的统计结果；若该群尚未开启统计，必须明确提示当前未开启。
- 统计项约定：基础统计项固定为“被艾特次数最多的人”“贴表情最多的人”“被贴表情次数最多的人”；若该群实际开放 `/盒`，则额外展示“/盒 次数最多的人”与“被 /盒 次数最多的人”。
- 贴表情口径约定：当前按消息段类型 `face`、`mface`、`marketface` 统计“贴表情”；“被贴表情”按同一条含表情消息里被 `@` 的成员累计，不统计 `@all`，也不统计机器人自己。
- bot 干扰过滤约定：若消息发送者或被 `@` 对象的群昵称/昵称为 `ARClass` 或 `Yurisaki`，则其相关艾特统计、贴表情统计和被贴表情统计都不计入每日统计。
- `/盒` 统计接入约定：谐音盒成功输出后，调用者累计到“/盒 次数”；若命中的结果绑定了群友，则对应绑定对象累计到“被 /盒 次数”。
- 持久化约定：群统计的开启状态和当天计数都落盘到 `data/group_daily_stats/`；重启后需继续保留开启状态，但日结后必须清空当天计数。
- 调度约定：日结逻辑应由机器人逻辑层自行定时驱动，不依赖外部 cron；若机器人在结算时不在线，则不会补发离线期间的历史统计。

### 2026-06-05 11:52

- 笑话自动归档约定调整：若群内任意普通消息包含“张老师”“张师”“张雪峰”“雪峰”“巧乐兹”“雪碧”中的任一关键词，则无需手动上传，也应被自动归档到 `张雪峰` tag，并正常提示入库结果。
- 同步兼容约定：若用户手动上传笑话时引用内容命中同一批关键词，则依旧强制自动归档到 `张雪峰` tag，不受显式填写其他 tag 影响。

### 2026-06-05 12:08

- 奶龙查询能力扩展：新增 `@机器人 /编号查找奶龙 <序号>` 与 `@机器人 /模糊查找奶龙 <名称关键字>`，分别按库存序号与奶龙名称进行查找。
- 奶龙群开关扩展：新增 `@机器人 /奶龙套皮` 与 `@机器人 /奶龙去皮` 两个管理员命令，用于控制当前群的 `/随机奶龙` 是否以合并转发消息形式发送。
- 持久化约定补充：奶龙套皮的群级开关状态需单独落盘，机器人重启后仍保持各群原有套皮状态。

### 2026-06-05 10:15

- 文档维护约定调整：`AGENTS.MD` 在重构时必须保留原有仍然有效的工程约束语言与历史更新日志，不应因结构整理而整体丢弃历史记录。
- 脱敏约定补充：历史日志若出现真实管理员账号、群号、访问令牌或其他敏感信息，应统一替换为 `ADMIN_QQ`、群白名单或等价占位描述，但保留原本约束语义。

### 2026-06-05 10:42

- 群插件范围约定调整：当前版本对所有群默认只开放杀戮尖塔猜卡插件与 `/猜卡 help`；奶龙、谐音盒、昔涟、笑话梗库及其帮助入口，仅在 `.env` 中 `EXTRA_PLUGIN_GROUP_IDS` 指定的群里开放。
- 总帮助约定调整：`@机器人 /help` 在普通群只展示猜卡帮助入口，在 `EXTRA_PLUGIN_GROUP_IDS` 指定群中才展示完整插件帮助列表。

### 2026-06-04 02:18

- 新增需求：实现“杀戮尖塔猜卡牌”插件，数据源默认读取仓库内 `data/sts_card_guess/cards/` 下的卡牌 JSON，也允许通过 `.env` 中 `STS_CARD_DATA_DIR` 覆盖，并独立维护在 `src/plugins/sts_card_guess/` 下。
- 触发约定：猜卡主命令继续采用 `@机器人` 触发，支持 `@机器人 /猜卡` 开局、`@机器人 /提示` 推进阶段、`@机器人 /结束猜卡` 结束本局；帮助入口为无需 `@` 的 `/猜卡 help`。
- 群状态约定：同一个群同一时间只允许存在一局进行中的猜卡；若当前群已有未结束对局，则新的 `/猜卡` 或 `/猜卡测试` 必须直接拒绝开启。
- 提示约定：开局先从“卡名字数、类型、来源、费用”四项基础提示中随机给出三项；由于卡名本体必须隐藏，不允许直接公布名称，因此名称维度统一用“卡名字数”替代；10 秒后若仍无人猜中，再补出剩余那一项基础提示。
- 描述遮罩约定：卡牌描述中的非数字、非标点字符在初始阶段必须隐藏为 `_`，且显示时以下划线与字符逐项空格分隔，避免 QQ 客户端把连续下划线渲染成一整条线。
- 进阶提示约定：基础提示补全后，若仍未猜出，则每隔 60 秒按“开局初始隐藏字总数”的 25% 固定揭示一批字符，总共只揭示 4 批；第 4 批揭示完成后仍无人猜中时，必须立即宣布失败并结束本局。
- 猜测约定：用户通过 `@机器人 卡牌完整名称` 进行作答；猜对则立即宣布成功并结束，猜错只提示失败但游戏继续。
- 手动推进约定：`@机器人 /提示` 可跳过等待时间直接进入下一阶段，并在进入下一阶段后将计时器统一重置为 60 秒。
- 测试接口约定：提供仅管理员可用的 `@机器人 /猜卡测试 <卡牌名或ID>` 调试入口，用指定卡牌直接开局，便于验证提示流程、定时器与答案判定。
- 本地测试约定：除群内调试入口外，仓库还应提供一个不依赖 NapCatQQ / OneBot 的本地 CLI 测试脚本，直接调用猜卡核心逻辑，便于在纯本地环境下验证描述遮罩、固定批次提示推进与答案判定；该脚本只保留 `/提示`、`/答案`、`/退出` 三个本地调试命令。
- 帮助聚合约定：总 `@机器人 /help` 需要同步纳入 `/猜卡 help` 入口。

### 2026-06-03 11:12

- 新增需求：接入基于第三方大模型 API 的“昔涟口吻”文本改写插件，并与现有 NoneBot2 指令体系解耦，独立放入 `src/plugins/` 下的新插件目录中维护。
- 指令约定：用户引用一段不超过 200 个字符的文本后，可通过 `@机器人 /昔涟改写` 获取一段不超过 200 字的昔涟口吻改写文本，也可通过 `@机器人 /昔涟回复` 获取一段不超过 200 字的、以昔涟口吻对引用文本进行回应的回复文本。
- 风格约定：昔涟口吻的系统提示必须显式约束“少女感、柔和抒情、偏星穹铁道昔涟风格”，并严格遵守 `U+266A`（即 `♪`）符号要求；允许适度插入该符号，但全文最后必须以单个 `♪` 结尾，且以 `♪` 结尾的句子后不得再追加任何标点，也不允许误用 `🎵` 等其他音符表情替代。
- 管理约定：新增 `@机器人 /开启昔涟模式` 与 `@机器人 /关闭昔涟模式` 两个管理命令，仅 `.env` 中 `ADMIN_QQ` 对应的管理员可执行；非管理员触发时统一回复“您没有管理权限，无法开启”。
- 开关约定：昔涟模式关闭时，普通用户触发 `/昔涟改写` 与 `/昔涟回复` 不应调用 API，也不应继续处理改写逻辑，而是直接提示该模式当前未开启。
- 限流约定调整：昔涟 API 接口不再执行“1 分钟 3 次”的全局调用上限，后续按模式开关和接口可用性直接处理请求。
- 异常处理约定：调用第三方文本生成 API 时，必须设置超时、捕获网络异常与非 2xx 响应，并在失败时返回明确的降级提示，不能让机器人因接口失败而报错中断。
- 接口形态约定：昔涟文本生成功能默认按 Responses API 结构发起请求，优先使用 `POST /v1/responses`，通过 `instructions + input` 组织提示词与用户输入，并优先读取 `output_text` 及 `output[].content[].text` 等 Responses 风格输出字段。

### 2026-06-04 00:10

- 新增需求：实现“笑话梗库”插件，允许用户通过引用消息自行上传笑话，并支持通用集合与 tag 专属集合两类归档方式。
- 触发约定：笑话相关命令全部改为无需 `@` 机器人即可直接触发，包括 `/上传笑话`、`/随机笑话`、`/模糊查找笑话`、`/编号查找`、`/删除笑话` 与 `/笑话 help`。
- 上传约定：用户引用一条聊天消息后发送 `/上传笑话` 可上传到通用笑话库；发送 `/上传笑话 <tag>` 可上传到对应 tag 集合；每条笑话都必须分配全局唯一编号，并记录提供人 QQ 与提供时间。
- 去重约定：笑话去重必须基于“引用消息规范化内容”的内容指纹完成，优先将消息段序列规范化后计算哈希，避免每次上传时做低效的全量逐段比对；重复笑话不应重复入库，而应返回已有编号与元信息。
- 内容持久化约定：笑话库存储到 `data/joke_library/index.json`；若引用消息中包含图片，则应尽量下载并落盘到 `data/joke_library/images/` 后再保存索引，避免后续随机展示时依赖临时 URL 失效。
- 查询约定：`/随机笑话 <tag可选>` 从指定 tag 或全部笑话中随机抽取一条；`/模糊查找笑话 关键字段` 仅接受不超过 10 个字的查询词，并返回当前匹配度最高的一条笑话；`/编号查找 编号` 按全局编号精确返回单条笑话。
- 权限约定：`/删除笑话 编号` 仅允许 `.env` 中 `ADMIN_QQ` 对应的管理员执行；非管理员调用时必须明确提示没有删除权限。
- 展示约定：随机、模糊查找与编号查找返回的笑话均需附带显示归属 tag、提供人 QQ、提供时间和笑话编号；无 tag 的笑话统一按“通用”展示。

### 2026-06-04 00:32

- 笑话随机展示约定调整：`/随机笑话` 与 `/随机笑话 <tag>` 返回时不再附带完整元信息，只保留笑话内容本体和笑话编号，避免消息过于臃肿。
- 帮助入口约定：奶龙、盒、昔涟、笑话四个插件都必须提供各自的帮助入口，分别为 `/奶龙 help`、`/盒 help`、`/昔涟 help`、`/笑话 help`，且这四个帮助入口均不需要 `@` 机器人。
- 总帮助约定：新增仅在 `@机器人 /help` 时触发的总帮助入口；该总帮助只输出各分支插件 help 指令，不展开详细说明，以避免与群内其他 bot 的 `/help` 冲突。

### 2026-06-04 01:08

- 奶龙去重约定：`nailoong_memory` 插件上传时必须执行双层去重。图片型奶龙先按原始图片字节计算 `sha256` 做精确去重，再按图片感知哈希 `dHash` 做近似去重；当两张图尺寸一致且 `dHash` 距离足够小，也应判定为重复。
- QQ 表情去重约定：对于 `face`、`mface`、`marketface` 等段，至少要保留消息段规范化内容指纹去重；若消息段可提取可下载图片地址，则应尽量继续接入同一套 `sha256 + dHash` 图片去重流程，以提升“不同 QQ 表情但图片相同”场景下的识别率。
- 依赖约定：项目新增 `Pillow` 作为奶龙感知哈希计算依赖；后续任何与本地图片解码、缩放、像素级哈希相关的逻辑默认复用该依赖，不再重复引入新的图像库。

### 2026-06-03 10:55

- 英文整词边界约定调整：谐音盒中的英文整词命中不再按“任意非字母字符”切分，而是明确只将“空格、中文字符、文本边界”视为英文单词边界；因此中文与英文直接相邻时仍应识别该英文单词，但带连字符等英文内部样式不应被拆成多个整词命中。
- 纯英文首字母匹配约定确认：即使被引用文本完全不含中文，只要能解析出多个英文单词，也必须继续允许其按单词首字母参与 `/谐音盒` 与 `/盒` 的组合匹配，而不只是支持英文整词直接命中。

### 2026-06-03 10:18

- 仓库发布约定：项目默认采用 `MIT License`，便于 bot 项目在 NoneBot2 / NapCatQQ 生态中被复用、修改与分发。
- Git 管理约定：许可证文件直接维护在仓库根目录 `LICENSE`，避免依赖 GitHub 创建仓库时自动生成，确保本地仓库与远端仓库的法律文本一致。

### 2026-06-03 10:26

- 管理员配置约定：奶龙库存管理权限不再硬编码在插件内，统一改为从 `.env` 中读取 `ADMIN_QQ`，便于 fork 本项目的其他使用者改成自己的管理员账号。
- 模板配置约定：`.env.example` 必须包含 `ADMIN_QQ` 示例项，避免 fork 后的部署者遗漏权限配置。
- 文档重构约定：`README.md` 以“fork 后独立部署”为主线重写，优先说明环境准备、NapCatQQ 配置、`.env` 字段说明、权限配置、命令使用和常见问题。

### 2026-06-03 10:34

- 群范围约定：谐音盒功能不是全局开放，必须通过 `.env` 中的群号白名单进行限制；只有指定群内才能触发 `/谐音盒` 与 `/盒`。
- 首字母绑定约定：`/添加首字母` 支持在新增时可选绑定一个群成员；若首字母已存在，则通过 `/绑定群友 首字母 @群友` 为该首字母补充或覆盖绑定关系。
- 展示约定：谐音盒命中结果若存在绑定群友，应输出为 `<原结果>（群昵称）`；不允许再次艾特该群友，必须直接显示群昵称或可退化为用户卡片名/昵称，避免打扰。

### 2026-06-03 10:40

- 解绑约定：首字母若已绑定群友，必须支持通过 `/解绑群友 首字母` 清除该绑定关系，而不删除首字母本身。
- 展示回退约定：解绑后，谐音盒输出应立即恢复为不带群昵称的原始格式，`/首字母列表` 中对应条目也不再显示绑定对象。

### 2026-06-03 10:47

- 英文匹配约定扩展：谐音盒保留原有“英文单词按首字母参与组合匹配”的能力，同时新增“英文单词整体命中”能力；若引用文本中存在以空格分隔的英文单词，且该单词本体小写后恰好存在于首字母库中，也应直接命中。
- 纯英文输入约定：谐音盒不再强依赖中文字符；当引用文本没有中文、但存在以空格分隔的可识别英文单词时，也允许继续执行匹配逻辑。

### 2026-06-03 08:45

- 谐音盒防刷屏约定：当 `/谐音盒` 或 `/盒` 命中结果超过 10 条时，只输出其中 10 条即可，不要求固定选择规则。
- 输出控制约定：匹配逻辑仍可保留完整命中能力，但对外回复必须做结果上限截断，避免群聊中一次性刷出过多内容。

### 2026-06-03 09:02

- 谐音盒高并发保护约定：当短时间内 `/谐音盒` 或 `/盒` 请求过多时，应通过有限缓冲区与并发上限进行削峰，避免事件循环被同类请求瞬时打满导致机器人卡死。
- 过载反馈约定：若谐音盒等待队列已满，后续请求不再继续排队，应直接回复“当前盒子太忙，请稍后再试。”之类的提示，尽快失败而不是无限堆积。
- 单条消息冷却约定：同一条被引用消息在 5 秒内只允许成功触发一次谐音盒；后续用户再次对同一条消息触发时，机器人只回复“已经盒过了”。

### 2026-06-03 08:49

- 谐音盒展示策略调整：当命中结果超过 10 条时，不再固定取前 10 条，而是从全部命中结果中随机抽取 10 条返回，避免每次输出都完全相同。
- 测试约定：随机抽样逻辑在单元测试中应允许注入固定随机源，以保证测试可重复且不会因随机性导致不稳定。

### 2026-06-03 08:55

- 谐音盒去重体验约定：对于同一条引用文本与同一批首字母模式，在短时间内重复触发时，应尽量避免连续返回完全相同的组合集合，优先轮换尚未展示过的命中结果。
- 首字母长度约定调整：`/添加首字母` 与 `/删除首字母` 不再限制为固定 3 位，改为支持 2 到 5 个英文字母。
- 匹配长度约定调整：谐音盒匹配时，候选单元数量必须与首字母模式长度一致；也就是说 2 位模式匹配 2 个候选单元，5 位模式匹配 5 个候选单元。

### 2026-06-03 08:41

- 新增需求：为姓名首字母库补充查看与删除管理命令，避免首字母数据长期积累后难以维护。
- 管理约定：支持 `/首字母列表` 查看全部首字母模式，支持 `/删除首字母 yzh` 删除指定三字母模式。
- 匹配能力扩展：谐音盒不再只识别中文单字，也应识别引用文本中的英文单词首字母，并允许英文单词与中文字符混合组成三位首字母结果。
- 输入解析约定：谐音盒匹配时，候选单元为“中文单字”或“连续英文单词”，按原始顺序选取 3 个候选单元进行首字母匹配。
- 输出约定：命中结果展示时保留原始候选单元文本，因此混合结果可能形如 `<杨A魂>`、`<you之魂>` 等，具体取决于引用文本内容。

### 2026-06-03 08:36

- 谐音盒长度限制调整：由于当前实现完全基于本地拼音计算与本地姓名库匹配，不调用外部 API，故将引用文本的有效中文字符上限从 20 放宽到 40。
- 提示文案约定：当输入超过限制时，应明确提示“当前只处理不超过 80 个字符的文本”。

### 2026-06-03 03:09

- 谐音盒输出格式调整：命中结果统一改为 `盒出了 <xxx>` 的样式，使用尖括号包裹结果字符组合，提升可读性与视觉分隔。

### 2026-06-03 03:10

- 谐音盒多结果排版优化：当存在多个命中结果时，统一使用 `盒出了：` 作为标题，随后每行展示一个 `<xxx>`，减少重复前缀，提高 QQ 聊天窗口中的可读性。

### 2026-06-03 08:33

- 谐音盒新增快捷入口：当用户引用文本后直接发送 `/盒`，无需 `@` 机器人，也应触发谐音盒匹配。
- 兼容约定：原有 `@机器人 /谐音盒` 入口继续保留；新增快捷入口仅对 `/盒` 生效，不放开其他命令的无 `@` 触发。

### 2026-06-03 03:03

- 新增需求：实现“谐音盒”功能和姓名首字母库。
- 指令约定：用户可通过 `@` 机器人发送 `/添加首字母 yzh` 的方式，将三位首字母模式加入姓名库；该功能当前不记录添加者和添加时间。
- 数据约定：姓名首字母库存储为本地持久化文本或 JSON 索引，重启后不丢失。
- 谐音盒触发约定：用户必须引用一段文本，并发送 `@` 机器人 `/谐音盒`。
- 输入限制约定：被引用文本只在“低于 20 个中文字符”时参与匹配；超过限制或无有效中文文本时，机器人应明确提示。
- 匹配约定：从被引用文本中按原顺序选择 3 个中文字符，若其拼音首字母依次等于姓名库中的某个三字母模式，则输出 `盒出了<xxx>`，其中 `xxx` 为被选中的 3 个字符。
- 输出约定：若存在多个满足条件的结果，必须全部输出；若无结果，应明确提示“没盒出来”之类的失败信息。

### 2026-06-03 02:46

- 新增需求：为奶龙库存管理补充 `/删除奶龙 最近一个` 与 `/撤销删除奶龙`。
- 权限约定：上述新命令继续仅允许 `ADMIN_QQ` 使用。
- 删除语义约定：`/删除奶龙 最近一个` 删除当前库存中最后添加的一条记录。
- 撤销语义约定：`/撤销删除奶龙` 只恢复最近一次删除操作中的最后一条记录，不要求支持多步批量撤销。
- 可恢复删除约定：图片型记录删除时不直接销毁文件，而是移动到本地回收区以支持恢复；QQ 表情型记录则通过索引恢复。

### 2026-06-03 02:41

- 新增需求：为奶龙表情包库存补充管理命令 `/奶龙列表` 与 `/删除奶龙`。
- 权限约定：仅允许 `ADMIN_QQ` 使用奶龙库存管理命令，其他用户即使 `@` 机器人也不能进行查看或删除。
- 列表约定：`/奶龙列表` 需返回带序号的库存视图，每条至少包含奶龙名称、资源类型、添加者 QQ、添加日期，便于后续按序号删除。
- 删除约定：`/删除奶龙 <序号>` 删除单条记录；`/删除奶龙 <名称>` 按名称精确匹配并删除全部同名记录。
- 资源清理约定：删除图片型奶龙记录时，必须同步删除 `data/nailoong/images/` 下对应文件；删除 QQ 表情型记录时只删除索引。

### 2026-06-03 02:31

- 新增需求：实现 `nailoong_memory` 奶龙表情包记忆插件。
- 功能约定：用户 `@` 机器人并引用一张奶龙表情包后，发送 `/添加奶龙 <名称可选>`，机器人将该图片保存到本地并记录名称、添加者 QQ、添加日期。
- 功能约定：用户 `@` 机器人发送 `/随机奶龙` 时，机器人从本地库存中随机发送一张奶龙表情包，并附带名称、添加者 QQ、添加日期。
- 数据落盘约定：奶龙图片保存到 `data/nailoong/images/`，元数据索引保存到 `data/nailoong/index.json`，当前阶段不引入数据库。
- 消息解析约定：添加功能依赖 OneBot V11 的回复消息解析，必须从 `event.reply.message` 中提取图片段；若引用消息中无图片，机器人应明确提示。
- 文档约定：`README.md` 必须给出从 NapCatQQ 小号登录、OneBot V11 正向 WebSocket 配置、`.env` 配置、Bot 启动到命令验证的完整步骤。

### 2026-06-03 02:30

- 确立了项目工程骨架，创立了 `AGENTS.md` 协作手册。
- 设定基础技术栈为 Python + NoneBot2 + NapCatQQ。
- 规划了 `basic_reply` 与 `vision_api` 两个核心插件目录层级，明确了异步请求与环境变量解耦的硬性要求。
- 下一步计划：完成依赖安装，跑通 Hello World 与本地图片的 `MessageSegment.image` 发送逻辑。
